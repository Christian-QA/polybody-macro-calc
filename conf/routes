# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                                   controllers.HomeController.index()
GET     /explore                            controllers.HomeController.explore()
GET     /gender                             controllers.HomeController.gender()
GET     /age                                controllers.HomeController.age()
GET     /height                             controllers.HomeController.height()
GET     /current_weight                     controllers.HomeController.currentWeight()
GET     /target_weight                      controllers.HomeController.targetWeight()

GET     /getUserDetails/:username           controllers.UserController.getUserDetails(username: String)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file                       controllers.Assets.versioned(path="/public", file: Asset)
